# Machine Learning Labs

Lab work for the Machine Learning course.

| Folder | Contents |
|--------|----------|
| `lab1/` | Intro exercises (`36_code1.py`, `36_code2.py`, `labb1.py`) |
| `lab2/` | Regression problems on the CCPP (Folds5x2_pp) dataset |
| `lab3/` | MangoLeafBD image dataset experiments (dataset not tracked — too large for GitHub) |
| `lab5/` | Perceptron & logistic regression on the diabetes dataset |

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate        # Windows
pip install -r lab5/requirements.txt
```
